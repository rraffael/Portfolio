export default function WorkSection({ t }) {
  return (
    <div className="section-content">
      <div>
        <h1>{t('work.title')}</h1>
        <p>{t('work.description')}</p>
      </div>
    </div>
  )
}
